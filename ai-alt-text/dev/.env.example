# Database
DATABASE_URI=mongodb://127.0.0.1:27017/ai-alt-text-dev

# Payload
PAYLOAD_SECRET=your-secret-key-change-in-production

# OpenAI API
# For development, can be any value since tests mock the API
# For manual testing with real API, use real key
OPENAI_API_KEY=test-key-or-real-api-key

# Plugin Configuration
# URL where images are served from (for thumbnail URL generation)
NEXT_PUBLIC_CMS_URL=http://localhost:3000

# Model to use for generation (optional - defaults to gpt-4o-mini)
NEXT_PUBLIC_OPENAI_MODEL=gpt-4o-mini

# Max concurrent requests for bulk generation (optional - defaults to plugin config)
OPENAI_CONCURRENCY=5

# Next.js
NODE_ENV=development
